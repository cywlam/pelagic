# Pelagic — Claude Instructions

## Project Context
Full project overview, architecture, module map, active areas, and external services:
→ [resources/project-context.md](resources/project-context.md)

## Database Schema
All tables, columns, TypeScript types, indexes, and migration history:
→ [resources/database-schema.md](resources/database-schema.md)

Consult this file before writing any SQL queries, Tauri commands that touch the DB, or TypeScript types related to stored data. Schema changes require a new migration entry (current version: 9).
